@model IEnumerable<CompanyJSL.Models.Facturas>

@{
    ViewBag.Title = "Factura";
}

<h2>Factura</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Código)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Numerodepago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Código)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idProducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Numerodepago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Código_de_facturas }) |
            @Html.ActionLink("Details", "Details", new { id=item.Código_de_facturas }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Código_de_facturas })
        </td>
    </tr>
}

</table>
